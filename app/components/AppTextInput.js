import React, {useState} from 'react'
import {StyleSheet, Text, View, TextInput} from 'react-native'

// recieves props [style, onKeyPress, placeholder, value]

const AppTextInput = ({style, onKeyPress, placeholder, value}) => {


    return('df')
}

export default AppTextInput;const styles = StyleSheet.create({})
